<!-- 我觉得这个方案**方向很好，包装也很适合答辩**，但它有一点“AI 味”：说得很满，实际落地时容易超时。我们可以保留它的主线，但把任务压实一点。

最值得保留的是这三点：

```text
1. 把树形 KG 增强成网状 KG
2. 用 GraphRAG 做智能助教/学习路径推荐
3. 三人分工：工程、数学质量、展示
```

但我会做几个调整。

**1. “GraphRAG”别讲太大**

你们现在的数据主要是知识点树，没有教材全文 chunk、定理文本、例题文本、公式证明文本。所以严格意义上的 GraphRAG 还不完整。

更稳的说法是：

```text
基于课程知识图谱增强的 KG-grounded 数学助教系统
```

如果你们后面真的把 `kg.txt` 或讲义文本分块接进去，再说 GraphRAG 就更站得住。

答辩里可以说：

```text
我们采用 GraphRAG 思想：先在图谱中检索相关知识点与关系路径，再将结构化图谱上下文提供给大模型生成回答。
```

这比直接宣称完整 GraphRAG 更稳。

**2. “把课本讲义分块喂给大模型自动抽取关系”要谨慎**

如果你们现在没有结构化讲义，直接从文本抽取会很费时间。更推荐：

```text
先基于现有知识点名称 + 层级关系生成候选关系
再用数学同学审核 100 条
再用 gold set 优化 prompt
```

这条路更短，也能形成实验闭环。

后续增强可以再加：

```text
如果有讲义文本，就把讲义片段作为 evidence 加入候选关系生成。
```

**3. 成员 C 的任务不要写“数据清洗 + UI + PPT”太散**

“啥都能干但时间少”最好不要给太多工程活。否则最后大概率还是你做。

更合理是：

```text
成员 C：展示与实验结果负责人
```

具体包括：

```text
PPT
流程图
Neo4j / UI 截图
实验表格
答辩演示脚本
可视化页面可选
```

如果时间够，再加一个 Streamlit/Gradio 页面。不要把可视化作为必须项。

**4. 数学同学的任务写得不错，但要更具体**

他不只是“审核逻辑正确性”，而是产出明确材料：

```text
关系类型定义表
100 条 gold set 审核结果
错误类型统计
5-10 个测试问题
答案合理性评分
```

这样他的贡献可见、可评估。

**我建议改成这个最终版本** -->

# 基于知识图谱增强的智能数学助教与学习路径推荐系统

## 技术路线
原始课程知识点树
  ↓
大模型生成候选语义关系
  ↓
数学专家审核形成 gold set
  ↓
用 gold set 优化 prompt
  ↓
二次生成增强关系
  ↓
导入 Neo4j，形成增强 KG
  ↓
KG-grounded LLM 问答与学习路径推荐


## 三人分工

你：Pipeline & Reasoning
- CSV / Neo4j 数据处理
- Qwen 自动生成候选关系
- prompt v1 / v2 对比
- KG-grounded 问答原型
- 输出推理路径

数学同学：Ontology & Evaluation
- 定义关系类型
- 审核 100 条 gold set
- 标注错误类型
- 设计 5-10 个测试问题
- 给问答结果打分

成员 C：Presentation & Demo
- PPT 和报告
- 方法流程图
- 实验结果表格
- 截图和演示脚本
- 可选：简单可视化页面


## 实验设计
Prompt v1 生成 100 条候选关系
数学同学审核，得到准确率和错误类型

用 gold set 优化 prompt
Prompt v2 再生成 100 条候选关系
再次审核或抽样审核
比较 v1/v2 准确率


## 下游应用展示

用户输入：我学泰勒公式总是不会用

系统输出：
1. 命中知识点：泰勒公式
2. 前置知识：高阶导数、函数极限、导数运算
3. 相关方法：洛必达法则、等价无穷小、局部近似
4. 推荐路径：导数定义 -> 高阶导数 -> 泰勒公式 -> 泰勒展开应用
5. 图谱依据：列出 used_edges
6. 大模型解释：只基于上述图谱事实生成


把重点放在：

```text
AI 自动生成关系是否变准了？
增强关系是否让学习路径推荐更合理？
LLM 回答是否能引用图谱路径？
```

这三个问题答清楚，你们项目就很扎实。



## 小组工作流程

### 0. 可选：预热 Ollama 模型

本地模型第一次加载会比较慢。如果发现第一批候选生成卡住很久，可以先预热：

```bash
ollama run qwen3.5:9b
```

随便问一句，确认模型能回答后退出。也可以用较快的模型先跑流程：

```bash
qwen2.5:7b
```

### 1. 生成 Prompt v1 候选关系

先小规模试跑，确认速度和输出格式：

```bash
python3 code/kg_workflow.py generate-candidates --model qwen3.5:9b --prompt-version v1 --limit 20 --batch-size 4 --keep-no-relation
```

如果 9B 太慢，可以先用 7B 跑通工作流：

```bash
python3 code/kg_workflow.py generate-candidates --model qwen2.5:7b --prompt-version v1 --limit 20 --batch-size 4 --keep-no-relation
```

正式生成第一批候选时再扩大数量：

```bash
python3 code/kg_workflow.py generate-candidates --model qwen3.5:9b --prompt-version v1 --limit 120 --batch-size 4 --save-every 10 --timeout 180 --keep-no-relation
```

说明：

```text
--batch-size 4      一次让模型判断 4 对知识点，避免 1 条 1 调用太慢
--save-every 10     每累计 10 条结果自动保存，防止中途白跑
--timeout 180       单次模型请求最长等待 180 秒
```

如果只是想测试 UI，不调用大模型：

```bash
python3 code/kg_workflow.py generate-candidates --prompt-version v1 --limit 20 --batch-size 4 --no-llm
```

输出文件：

```text
csv_data_annotated/candidate_relations_v1.csv
```

### 2. 启动审核 UI

另开一个终端运行：

```bash
python3 code/annotation_server.py
```

打开 UI：

```text
http://127.0.0.1:8765
```

然后数学同学：

```text
导入候选 -> 审核 accepted/rejected -> 填人工标签/错误类型/说明 -> 导出 Gold Set
```

导出后会得到：

```text
csv_data_annotated/gold_set.csv
```

### 3. 用 Gold Set 生成 Prompt v2 候选关系

先小规模试跑：

```bash
python3 code/kg_workflow.py generate-candidates --model qwen2.5:7b --prompt-version v2 --gold-set csv_data_annotated/gold_set.csv --limit 20 --batch-size 4 --output csv_data_annotated/candidate_relations_v2.csv
```

正式跑：

```bash
python3 code/kg_workflow.py generate-candidates --model qwen3.5:9b --prompt-version v2 --gold-set csv_data_annotated/gold_set.csv --limit 120 --batch-size 4 --save-every 10 --timeout 180 --output csv_data_annotated/candidate_relations_v2.csv
```

把 v2 候选导入审核 UI：

```bash
python3 code/kg_workflow.py import-candidates csv_data_annotated/candidate_relations_v2.csv
```

### 4. 评估

评估含义：拿 AI 的输出和数学同学的 Gold Set 对比，统计准确率和错误类型。

```bash
python3 code/kg_workflow.py evaluate-gold --gold-set csv_data_annotated/gold_set.csv
```

如果评估不合格，就可以返回重新弄。闭环是: 
AI 生成候选
  ↓
人工审核
  ↓
评估准确率和错误类型
  ↓
发现问题
  ↓
修改 prompt / 关系定义 / 候选范围
  ↓
重新生成
  ↓
再次评估


### 5. KG-grounded demo

当前 demo 的严格逻辑：

```text
程序负责：
1. 检索命中知识点
2. 检索相关图谱边 used_edges
3. 固定打印“命中知识点”和“图谱依据”

大模型负责：
1. 基于程序给出的图谱事实生成推荐学习路径
2. 基于程序给出的图谱事实生成简短解释
```

也就是说，不能让大模型自己编写“图谱依据”。图谱依据必须由程序从 KG 中确定性输出，否则无法证明回答真的来自知识图谱。

默认会使用：

```text
原始/导出的关系 CSV
  +
csv_data_annotated/annotation_state.json 中 status = accepted 的人工审核关系
```

也就是说，只要数学同学在 UI 中把候选关系标为 accepted，demo 就能直接用到，不一定必须先导出 CSV。

```bash
python3 code/kg_grounded_demo.py "我学泰勒公式总是不会用" --model qwen2.5:7b
```

如果要做对比实验，只使用原始树形图谱：

```bash
python3 code/kg_grounded_demo.py "我学泰勒公式总是不会用" --model qwen2.5:7b --raw-only
```

先不要急着比较大模型最终回答，应该先比较实际喂给大模型的图谱上下文：

```bash
python3 code/kg_grounded_demo.py "我学泰勒公式总是不会用" --show-context
python3 code/kg_grounded_demo.py "我学泰勒公式总是不会用" --show-context --raw-only
```

对比方式：

```text
raw-only：只使用原始 CONTAINS 层级关系
默认：原始关系 + 人工审核 accepted 的增强关系
```

如果两条 `--show-context` 输出完全一样，而最终回答不同，那么差异主要来自大模型生成随机性或表达差异，不应声称增强图谱在该问题上生效。

如果默认模式的 `--show-context` 比 raw-only 多出了 accepted 的增强关系，并且程序打印的“图谱依据”中出现了这些增强关系，才说明增强图谱对该问题起到了作用。

这个对比可以用来说明增强图谱是否让学习路径推荐更合理。

注意：目前 `candidate_relations_v1.csv`、`candidate_relations_v2.csv`、`gold_set.csv` 中暂时没有泰勒公式相关增强关系。因此，“我学泰勒公式总是不会用”这个问题目前主要展示原始树形 KG 的效果，不能用来证明增强图谱已经生效。

如果要展示增强图谱效果，需要选择已有 accepted 增强关系的主题，例如当前已有审核关系附近的：

```text
无穷小
由定义求导
```

或者先让数学同学审核几条泰勒相关增强关系，再用泰勒问题做增强前/增强后对比。

PPT 制作

---

> ⚠️ **本文档保留作为方案讨论背景。当前仓库已重构为「零数据库的本地静态 KG 浏览器」，详见 [README.md](README.md)。**
> 
> Neo4j、annotation_ui、kg_workflow（v1/v2 prompt + Ollama）、kg_grounded_demo 等组件全部移除；KG 数据由 `data/build_kg.py` 一次性生成，UI 由 `viewer/` 提供。

